# Database
DATABASE_URL=postgresql://forge:forge@localhost:5432/forge

# Auth
JWT_SECRET=change-me-in-production-use-64-char-random-string
JWT_EXPIRES_IN=7d

# Server
PORT=3001
NODE_ENV=development

# CORS (comma-separated for multiple origins)
CORS_ORIGIN=http://localhost:3000

# Google OAuth (optional — get from https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=

# Email SMTP (optional — without these, reset emails print to console)
# Works with any SMTP provider: Gmail, SendGrid, Mailgun, AWS SES, etc.
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=emailtest6633@gmail.com
EMAIL_SERVER_PASSWORD="ydbk smpz aaki uivg"
EMAIL_FROM="FORGE <noreply@forge.dev>"

# Frontend URL (for reset password links)
FRONTEND_URL=http://localhost:3000
